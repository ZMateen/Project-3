//resets the character select screen
import React from "react";
import "./ResetBtn.css";

{/*
make onClick function to reset the page
and insert the following code into button tag

onClick={function name}
*/}

const ResetBtn = props => (


<button>Reset</button>

);



export default ResetBtn;
